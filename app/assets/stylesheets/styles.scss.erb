navbar.navbar-default.static-top {
  display: flex;
  justify-content: space-between;
  padding: 1em 1em 1em 1em;
  border: none;

  .navlinks {
    a {
      padding-right: 1em;
    }
  }
  background: #BFDBf7;
}

ul {
  list-style: none;
}

.link{
	margin: 1.2em auto;
}

main{
  padding: 0em 2em 0em 2em;
}